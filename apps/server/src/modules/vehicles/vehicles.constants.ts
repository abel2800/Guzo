export const VEHICLES_MESSAGES = {
  FETCHED: 'Vehicle list fetched',
  FOUND: 'Vehicle found',
  CREATED: 'Vehicle created',
  UPDATED: 'Vehicle updated',
  DELETED: 'Vehicle deleted',
} as const;

export const VEHICLE_SEARCH_FIELDS = ["plateNumber","brand","model"] as const;
