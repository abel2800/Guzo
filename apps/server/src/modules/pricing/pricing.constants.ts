export const PRICING_MESSAGES = {
  FETCHED: 'PricingRule list fetched',
  FOUND: 'PricingRule found',
  CREATED: 'PricingRule created',
  UPDATED: 'PricingRule updated',
  DELETED: 'PricingRule deleted',
} as const;

export const PRICINGRULE_SEARCH_FIELDS = ["name"] as const;
