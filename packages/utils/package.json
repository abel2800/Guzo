{
  "name": "@delivery/utils",
  "version": "0.1.0",
  "private": true,
  "description": "Shared isomorphic utilities (formatting, geo, ids, guards).",
  "exports": {
    ".": {
      "types": "./src/index.ts",
      "default": "./src/index.ts"
    }
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "devDependencies": {
    "typescript": "^5.7.2"
  }
}
